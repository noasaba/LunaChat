/**
 * Stable, platform-independent integration contracts for LunaChat.
 * <p>Consumers must depend on this artifact as {@code compileOnly}/{@code provided}
 * and must not shade it, so the provider and consumer share class identity.</p>
 */
package com.github.ucchyocean.lunachat.api;
